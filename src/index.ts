export { compose, curry, isFunction, pipe, uncurry } from './function';
export {
  chain,
  deepFreeze,
  deepMerge,
  deepUnfreeze,
  getHash,
  isClass,
  isEqual,
  isPlainObject,
  omit,
  pick,
  select,
} from './object';
export type * from './types';
